@model AjadiForum.Models.ViewModels.ForumIndexModel

<h2> Forums</h2>

<table class="table table-hover" id="forumIndexTable">
   
    @foreach (var forum in Model.ForumList)
    {
        <tr>
            <td>
                <a asp-controller="Forum" asp-action="Topic" asp-route-id="@forum.Id">
                    @forum.Name
                </a>

            </td>
            <td>
                @forum.Description
            </td>

        </tr>

    }

</table>
